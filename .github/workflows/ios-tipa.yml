name: iOS Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v3
      - name: Set Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.app
      - name: Run Bootstrap Script
        run: echo "Workflow ready, IPA build not included here"
